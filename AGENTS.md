# 魔女协议 — 开发指南

> 当前为**地基阶段**：工程骨架与核心系统已就位，尚未进入可玩内容。所有 AI 开发必须遵守本指南。
>
> **核心原则：稳定 > 新功能，体验 > 系统数量，完成一个闭环 > 添加十个半成品。**

## 开工必读（每个任务开始前）

先读：

1. `docs/AI_GUARDRAIL.md`（强制红线：测试后门纪律 / 存档红线 / 开工三问）
2. `docs/DESIGN_DECISIONS.md`（制作人决策库：已拍板项 Agent 不得推翻）
3. `docs/开发约束与架构入口.md`（架构入口 + 新增系统检查清单）
4. `docs/dev/TestSystem.md`（**自动化测试体系契约**：探针/种子分离 + 编写范式 + 测试纪律；**涉及测试 / 写探针 / 改动验证必读**）
5. `docs/dev/SaveSystem.md`（**存档契约红线**：SaveVersion / 字段兜底 / 清档备份；**涉及存档改动必读**）
6. 对应任务文档（`docs/tasks/…`）

## 阶段定位

### 当前：地基阶段（冻结中）

工程骨架、autoload 单例、弹幕对象池 + Pattern 数学、探针测试体系、开发纪律文档均已就位并通过验证（5/5 探针 PASS）。

### 下一阶段：第一章垂直切片（规划中）

> 制作人决策 D-001（见 `docs/DESIGN_DECISIONS.md`）：从「加功能」转向「证明好玩」。优先做出一条可完整游玩的弹幕战斗闭环，而不是堆系统。

## 判断原则

- 判断标准不是「这个功能很酷」，而是「**玩家是否因此更想玩下去**」
- 一个功能若不能增强「战斗爽感 / 记忆叙事」，优先级降低
- 改地基前先问：会不会破坏已验证的 5 个探针？

## 优先级

### P0（最高）稳定性

- 修复 bug / 防止黑屏 / 防止存档损坏 / 防止场景切换异常
- 保持 5 个探针全绿

### P1 提升玩家体验

- UI 反馈 / 弹幕手感 / 操作反馈 / 音效反馈 / 新手引导

### P2 内容增强

- 敌人弹幕设计 / 记忆碎片剧情 / 美术替换

## 禁止事项（非常重要）

未经允许，**不要**：

- ❌ 引入后端 / 服务器架构（地基阶段明确不做）
- ❌ 大规模重构已验证的架构（autoload 分层 / 对象池 / 探针体系）
- ❌ 新增抽卡 / 付费 / 社交系统
- ❌ 为「看起来丰富」增加系统
- ❌ 在发布构建里保留测试后门

## Git 协作规则（硬性）

1. **禁止自动执行** `git add -A` / `git add .` / `git commit`（工具链若有自动提交/自动暂存功能，必须关闭，改为人工确认）。
2. **提交前必须确认** `git status`：核对本次要提交的只有自己改的文件。
3. **只提交自己修改的文件**：`git add <指定文件>`（逐个列路径）；**禁止** 宽泛暂存。
4. 提交统一由制作人执行：其余 Agent 只负责改代码 / 写文档，**禁止执行 `git add` / `git commit`**；任务完成后只报告「修改文件列表」。
5. 多 Agent 并行时，一个仓库同一时间只允许一个 Agent 持有 Git 写权限；写操作前先检查是否有其他 Agent 正在执行 git 进程（`Get-Process git`）。

## 验证纪律（改完必须验证）

- 任何 C# 改动 → 先 `.\tools\build.ps1` 确认 0 错误 0 警告
- 改动影响核心系统 → `.\tools\run_tests.ps1` 确认 5 探针全绿
- 沙箱环境（TRAE）下跑 Godot 时，`user://` 会被重定向到工程内 `.godot/userhome`（见 `tools/run_tests.ps1`），存档/日志/报告都在这里，不入库。
