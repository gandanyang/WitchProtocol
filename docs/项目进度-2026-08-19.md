# 《魔女协议》项目进度（2026-08-19 快照）

> 状态截至本文件日期。完整阶段天花板见 `docs/弹幕海与Boss演出系统-长期规划-v1.0.md`；
> 缺陷台账见 `docs/dev/BugLog.md`；复核报告见 `docs/dev/审查测试报告-2026-08-19.md`。

## 一、一句话定位

二次元魔女 × 高密度弹幕 × 华丽演出 × Boss 机制 × 音乐同步 的弹幕 RPG（参考《爆裂魔女》演出思路，不复制素材）。
技术栈：Godot 4.7.1 mono + C#/.NET 8，渲染 **GL Compatibility**（D-005），ComfyUI 美术管线。

## 二、阶段路线（长期规划 v1.0）

```
M1 魔女手感 → M1-PERF 性能稳定 → M2 割草+XP+升级 → M3 Boss基础战斗 → M3.5 Boss机制
→ M4 Boss演出系统 → M4.5 弹幕海 → M5 音乐同步 → M6 剧情化Boss战 → 正式Demo
```

**当前核心：M1 / M1-PERF（已完成主体），已开始 M2 铺路（工作树）。**

## 三、里程碑时间线（按 git 提交）

| 提交 | 阶段 | 内容 |
|---|---|---|
| `c595ed9` 01:39 | 地基 | 工程骨架 + autoload 单例 + 弹幕对象池/Pattern + 探针体系（5/5）+ 设计文档 + 美术资产 |
| `f97587d` 01:52 | 地基+ | enemy_config 数据驱动 + 第 6 探针 |
| `04fd327` 02:26 | **M1** | 手感垂直切片：P0-1 敌弹世界坐标 / P0-2 自机判定 3px / 数据驱动根因修复（.tres PascalCase）/ 探针 15/15 |
| `e70e5f7` 02:38 | 工具 | make_shortcut.py 桌面快捷方式 |
| `2f2c02f` 02:48 | M1 反馈层 | 手感调优 + 程序化音效 + 受击/无敌强化 + 战败 CG（并行协作） |
| `d29abaf` 02:56 | M1 反馈层 | 全屏战败 CG + 主菜单（继续 / 回主菜单） |
| `82ee07a` 03:03 | 缺陷收口 | **BUG-008~012 收口**（孤儿弹池兜底 / Zoom 复位 / 重开复位 / BossConfig PascalCase / 接触伤害） |
| `cd2008c` / `1978719` / `8513061` 03:11~03:36 | 资产 | 黎歌精灵：离线预处理（白底→透明高清）→ 俯视飞行视角（STG 自机标准）→ **战斗精灵 v3 定稿**（头朝上/裙后飘/星紫水晶翅膀） |
| `b899883` 03:58 | 修复 | Boss 漂移基准错误（入场收敛到屏幕中心再漂移） |
| —（工作树） | **M1-PERF** | 去物理体重构 / 自写碰撞 / 程序化贴图合批 / 压测体系（**未提交，见五**） |
| —（工作树） | **M2 铺路** | 敌人类别化（EnemyKind）+ Pickup 掉落 + 背景候选资产（**未提交**） |

> 渲染器 **GL Compatibility**（决策 D-005）已随 `04fd327`~`b899883` 区间入库（HEAD `project.godot` 即为 gl_compatibility）。

## 四、已完成任务盘点

| 项 | 状态 | 验证 |
|---|---|---|
| 地基：分层架构 / 单例 / 对象池 / Pattern / 探针体系 | ✅ 已提交 | 探针 5/5 |
| M1 手感：P0-1 敌弹世界坐标、P0-2 判定点、自动射击、移动手感、武器 1~3 级 | ✅ 已提交 | 探针 15/15 |
| 数据驱动根因修复（.tres 键名 → PascalCase + 哨兵探针） | ✅ 已提交 | `m1_enemy_move` 实测读取生效 |
| 反馈层：音效 / 受击白闪+震屏+HitStop / 无敌帧 / 战败 CG 演出 / 主菜单 | ✅ 已提交 | — |
| 缺陷台账 BUG-001~012 | ✅ 全部收口（含 wave_cleared 孤儿弹残留收尾） | 20/20 探针 |
| M1-PERF：去物理体（Node2D 化）、Main 自写距离碰撞、程序化共享贴图合批 | 🔸 已完成（工作树，未提交） | 20/20 探针 + 压测 9/9 |
| M1-PERF：压测体系（StressBench/StressLoad/bench.ps1，5000 弹跨红线） | 🔸 已完成（未跟踪/未提交） | 见「六」 |
| M2 铺路：EnemyKind 类型化（Chaser/Bomber/Sniper/Sprayer）+ 变体 .tres、Pickup 星之残片 | 🔸 起步（工作树，未提交） | — |
| 美术：黎歌立绘 / 战斗精灵 v3 定稿 / 战败 CG / 特效（hit_flash、kill_burst 等）/ 背景候选（bg_cand2_*、bg_cand3_*） | 🔸 资产就绪/候选（部分未提交） | — |

## 五、当前验证基线（本会话实测）

- `dotnet build`：**0 错误 0 警告**
- `tools/run_tests.ps1`：**20/20 探针全绿**（地基 6 + M1 手感 6 + 反馈 3 + weapon/boss_config/score/sfx + m1_orphan/m1_enemy_contact），退出无 RID 泄漏
- `tools/bench.ps1`（sweep 9 档）：**9/9 PASS**（常规 60、5000 弹 45 红线全过）
- 压测明细：100 弹 cpu 2.28ms → 2000 弹 9.02ms → 5000 弹 24.92ms（逼近 16.7ms 帧预算，**渲染侧需交互式 `tests/StressTest.tscn` 复核**）

> ⚠️ 提交边界提醒：**去物理体重构、自写碰撞、压测体系、EnemyKind/Pickup、背景候选资产均在工作树未提交**——这些改动已通过 20/20 探针与 9/9 压测，待制作人合流（按 AGENTS.md：提交统一由制作人执行）。

## 六、缺陷台账状态

- BUG-001~007：已修复（地基/M1 期间收口）
- BUG-008~012：已修复 + **残留收尾已实施**（`Main.ClearEnemyBullets()`：wave_cleared 时清孤儿弹）
- 复核证据：`docs/dev/审查测试报告-2026-08-19.md`（20/20 探针 + 9/9 压测 + 逐条代码证据）
- 遗留小项（非阻塞）：`ProbeM1OrphanBullet` 注释与实况不符（建议改注释）

## 七、决策与规划文档索引

- 决策：`docs/DESIGN_DECISIONS.md`（D-001 垂直切片 / D-002 技术栈 / D-003 user 目录 / D-004 沙箱 user:// / D-005 GL Compatibility）
- 规划天花板：`docs/弹幕海与Boss演出系统-长期规划-v1.0.md`（M1→Demo 全套阶段、性能红线、Boss 演出分层）
- 设计规范：`docs/魔女弹幕计划-设计规范-v0.1.md`；MVP 计划：`docs/MVP开发计划-v0.1.md`
- 测试契约：`docs/dev/TestSystem.md`；存档红线：`docs/dev/SaveSystem.md`

## 八、下一步建议（对齐 M1-PERF → M2）

1. **合流工作树 M1-PERF 改动**（去物理体/自写碰撞/压测体系）——已全绿，尽快入库避免长时间漂移。
2. **渲染侧复核**：跑交互式 `tests/StressTest.tscn` 确认真实帧率（5000 弹 CPU 侧已近预算）。
3. **推进 M2 割草闭环**：Pickup（星之残片）→ 经验 → 升级（复用结算三选一）→ 敌人类别（Sniper/Sprayer 等）接入 WaveManager 出波数据驱动（.tres 关卡表）。
4. 文档小项：修正 `m1_orphan` 探针注释；`docs/tasks/` 任务文档目录尚未启用，后续按需建立。
